{
  "name": "artifactx-packages-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium --with-deps",
    "serve:public": "python3 -m http.server 4173 --directory public"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
